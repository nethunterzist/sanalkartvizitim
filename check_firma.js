console.log('Veritabanı sorgulanıyor...'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function getFirma() { try { const firma = await prisma.firmalar.findUnique({ where: { id: 32 } }); console.log('Firma Bilgileri:', { id: firma.id, firma_adi: firma.firma_adi, telefon: firma.telefon, instagram: firma.instagram }); } catch (error) { console.error('Hata:', error); } finally { await prisma.$disconnect(); } } getFirma();
